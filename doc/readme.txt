---------------
mijigennerator （以下简称mijigen）


1. 印象笔记中，选择要导出的觅记（建议以月份为单位），菜单项中依次单击“文件”->“导出”，并选择“导出为多个网页文件”；
2. 将导出的路径，定位到mijigen的运行目录下的 html 文件夹；
3. 运行mijigen，选择 “1. gen；”，即可将 html 转换成 doc 文件，输出路径为运行目录下的 output_doc 文件夹；